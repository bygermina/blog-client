name: Deploy to DigitalOcean Droplet

on:
    push:
        branches:
            - main

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout Code
              uses: actions/checkout@v3

            - name: Set up SSH
              uses: webfactory/ssh-agent@v0.9.0
              with:
                  ssh-private-key: ${{ secrets.SSH_KEY }}

            - name: Deploy to Droplet
              run: |
               
                  ssh -T -o StrictHostKeyChecking=no root@${{ secrets.DROPLET_IP }} << 'EOF'
                  cd blog-client

                  echo 'export PATH="/root/.nvm/versions/node/v22.12.0/bin:$PATH"' >> ~/.bashrc
                  source ~/.bashrc
                  node -v

                  git pull https://${{ secrets.TOKEN }}@github.com/bygermina/blog-client.git main
                  npm install
                  npm run build:prod

                  pm2 flush

                  pm2 restart all --update-env || pm2 start server.js  --name 'my-blog-client' --update-env
                  EOF
